# Core Libraries
fastapi  # For building the API (if we choose a web API for the chatbot)
uvicorn[standard]  # ASGI server for FastAPI
requests  # For making HTTP requests to external APIs
pydantic  # For data validation and settings management
pydantic-settings # For settings management (used in config.py)

# Data Handling & Analysis
pandas  # For data manipulation and analysis (especially with financial/health data)
numpy  # For numerical operations
scikit-learn # For potential ML models (e.g., longevity risk, cost projections if we build custom models)

# Natural Language Processing (for chatbot)
# spaCy or NLTK could be options here, depending on the chosen chatbot framework
# rasa or ChatterBot for a more complete chatbot framework

# UI Layer
gradio

# Authentication
python-jose[cryptography]
passlib[bcrypt]

# Database (if we need persistent storage)
# sqlalchemy # For ORM with SQL databases
# psycopg2-binary # PostgreSQL adapter
chromadb # For vector database
# motor # Async MongoDB driver

# Other Utilities
python-dotenv  # For managing environment variables

# Placeholder for specific API client libraries if needed later, e.g.:
# yfinance
# bloomberg-sdk (if available and applicable)
